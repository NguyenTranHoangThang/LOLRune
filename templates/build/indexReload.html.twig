{% extends 'base.html.twig' %}

{% block title %}Hello BuildController!{% endblock %}

{% block body %}
    <style>
        .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
        .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
    </style>

    <div class="example-wrapper">
        <h1>Hello {{ controller_name }}! ✅</h1>
        <div id="timer"></div>
        <ul>
            <li>Your controller at <code><a href="{{ 'D:/New folder/collect-bot/src/Controller/BuildController.php'|file_link(0) }}">src/Controller/BuildController.php</a></code></li>
            <li>Your template at <code><a href="{{ 'D:/New folder/collect-bot/templates/build/index.html.twig'|file_link(0) }}">templates/build/index.html.twig</a></code></li>
        </ul>
        <script>

            String.prototype.toHHMMSS = function () {
                var sec_num = parseInt(this, 10); // don't forget the second param
                var hours   = Math.floor(sec_num / 3600);
                var minutes = Math.floor((sec_num - (hours * 3600)) / 60);
                var seconds = sec_num - (hours * 3600) - (minutes * 60);

                if (hours   < 10) {hours   = "0"+hours;}
                if (minutes < 10) {minutes = "0"+minutes;}
                if (seconds < 10) {seconds = "0"+seconds;}
                return hours+':'+minutes+':'+seconds;
            }

            var i = 10*60*1000;
            document.getElementById('timer').innerText = i;
            setInterval(function(){
                document.getElementById('timer').innerText = (document.getElementById('timer').innerText - 1) ;
            }, 1000);

            setTimeout(function () { window.location.reload(); }, i);
        </script>
    </div>
{% endblock %}